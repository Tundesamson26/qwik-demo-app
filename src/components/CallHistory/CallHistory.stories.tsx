// src/components/CallHistory/CallHistory.stories.tsx
import { CallHistory } from './CallHistory';

export default {
  title: 'Components/CallHistory',
  component: CallHistory,
};

export const Default = () => <CallHistory />;
